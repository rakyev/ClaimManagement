using System.Collections.Generic;
using System.Linq;
using ICM.Data.Business.RepositoryImplementation;

namespace ICM.Data.Business.BusinessObject
{
    public class SFSRestrictionItemBO : GenericRepository<SFSRestrictionItem>
    {
        //public override IQueryable<SFSRestrictionItem> GetAll(int skip = 0, int take = 50)
        //{
        //    return Context.Set<SFSRestrictionItem>().OrderBy(item => item.ri_pk).Skip(skip).Take(take);
        //}
    }
}
