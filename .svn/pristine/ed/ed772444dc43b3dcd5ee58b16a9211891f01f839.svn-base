using ICM.Data.Business.RepositoryImplementation;
using System.Linq;

namespace ICM.Data.Business.BusinessObject
{
    public class SFSUsersXPermissionsBO : GenericRepository<SFSUsersXPermission>
    {
        //public override IQueryable<SFSUsersXPermission> GetAll(int skip = 0, int take = 50)
        //{
        //    return Context.Set<SFSUsersXPermission>().OrderBy(item => item.up_pk).Skip(skip).Take(take);
        //}
    }
}
