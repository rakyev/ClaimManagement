using System.Linq;
using ICM.Data.Business.RepositoryImplementation;

namespace ICM.Data.Business.BusinessObject
{
   public class PrefixBO : GenericRepository<Prefix>
    {
       //public override IQueryable<Prefix> GetAll(int skip = 0, int take = 50)
       //{
       //    return Context.Set<Prefix>().OrderBy(item => item.PrefixID).Skip(skip).Take(take);
       //}
    }
}
