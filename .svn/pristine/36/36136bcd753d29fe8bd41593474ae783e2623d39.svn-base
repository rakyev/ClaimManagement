using System.Linq;
using ICM.Data.Business.RepositoryImplementation;

namespace ICM.Data.Business.BusinessObject
{
    public class LanguageBO : GenericRepository<Language>
    {
        //public override IQueryable<Language> GetAll(int skip = 0, int take = 50)
        //{
        //    return Context.Set<Language>().OrderBy(item => item.LanguageID).Skip(skip).Take(take);
        //}
    }
}
