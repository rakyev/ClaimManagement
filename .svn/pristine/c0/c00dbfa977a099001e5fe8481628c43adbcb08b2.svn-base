using ICM.Data.Business.RepositoryImplementation;
using System.Linq;

namespace ICM.Data.Business.BusinessObject
{
    public class SFSRolesBO :GenericRepository<SFSRole>
    {
        //public override IQueryable<SFSRole> GetAll(int skip = 0, int take = 50)
        //{
        //    return Context.Set<SFSRole>().OrderBy(item => item.rl_pk).Skip(skip).Take(take);
        //}
    }
}
