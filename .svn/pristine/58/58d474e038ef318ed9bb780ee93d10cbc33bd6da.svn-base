using ICM.Data.Business.RepositoryImplementation;
using System.Linq;

namespace ICM.Data.Business.BusinessObject
{
    public class SkedulexMessageBO: GenericRepository<SkedulexMessage>
    {
        //public override IQueryable<SkedulexMessage> GetAll(int skip = 0, int take = 50)
        //{
        //    return Context.Set<SkedulexMessage>().OrderBy(item => item.sm_pk).Skip(skip).Take(take);
        //}    
    }
}
