using System.Linq;
using ICM.Data.Business.RepositoryImplementation;

namespace ICM.Data.Business.BusinessObject
{
    public class InjuryCodeSeriesTypeBO : GenericRepository<InjuryCodeSeriesType>
    {
        //public override IQueryable<InjuryCodeSeriesType> GetAll(int skip = 0, int take = 50)
        //{
        //    return Context.Set<InjuryCodeSeriesType>().OrderBy(item => item.InjuryCodeSeriesTypeID).Skip(skip).Take(take);
        //} 
    }
}
