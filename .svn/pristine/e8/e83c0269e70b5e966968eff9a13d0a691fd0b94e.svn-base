using ICM.Data.Business.RepositoryImplementation;
using System.Linq;

namespace ICM.Data.Business.BusinessObject
{
   public class StandardInvoiceLineItemBO : GenericRepository<StandardInvoiceLineItem>
    {
       //public override IQueryable<StandardInvoiceLineItem> GetAll(int skip = 0, int take = 50)
       //{
       //    return Context.Set<StandardInvoiceLineItem>().OrderBy(item => item.StandardInvoiceLineItemID).Skip(skip).Take(take);
       //}
    }
}
