using System.Linq;
using ICM.Data.Business.RepositoryImplementation;

namespace ICM.Data.Business.BusinessObject
{
    public class InjuryCodeCategoryTypeBO : GenericRepository<InjuryCodeCategoryType>
    {
        //public override IQueryable<InjuryCodeCategoryType> GetAll(int skip = 0, int take = 50)
        //{
        //    return Context.Set<InjuryCodeCategoryType>().OrderBy(item => item.InjuryCodeCategoryTypeID).Skip(skip).Take(take);
        //}  
    }
}
